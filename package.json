{
  "name": "playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "headed": "npx playwright test --headed",
    "ui": "npx playwright test --ui",
    "config": "npx playwright test --config=playwright.config1.ts",
    "chromeProject": "npx playwright test --project=chromium",
    "firefoxProject": "npx playwright test --project=firefox",
    "webkitProject": "npx playwright test --project=webkit"

    
  },
  "keywords": [],
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.1.2"
  },
  "dependencies": {
    "exceljs": "^3.10.0"
  }
}
